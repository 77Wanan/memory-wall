services:
  memory-wall:
    build: ./backend
    container_name: memory-wall
    ports:
      - "8002:8002"
    volumes:
      - ./notes.db:/app/notes.db
      - ./backend/.env:/app/.env
    restart: unless-stopped
